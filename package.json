{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "---1---": "算法",
    "algorithm": "lerna run --stream start --scope=@claude-hub/algorithm",
    "-----": "删除所有依赖",
    "clean": "rm -rf node_modules && lerna clean -y"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.8"
  }
}
