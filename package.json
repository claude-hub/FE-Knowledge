{
  "name": "root",
  "private": true,
  "scripts": {
    "-------": "--hoist 重复的依赖提取到最外层的 node_modules 里，同时最外层的 package.json 也不会更新 dependency 信息",
    "hoist": "lerna bootstrap --hoist"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
